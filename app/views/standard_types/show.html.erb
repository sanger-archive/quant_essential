<%= link_to t('.edit'), edit_standard_type_path(@standard_type), class:"btn btn-default" %>
<%= render partial: 'unpaged_list', locals: { model_name: QuantType.model_name, items: @quant_types,  creation_link: new_quant_type_path } %>
<%= render partial: 'list', locals: { model_name: Standard.model_name, items: @standards,  creation_link: new_standard_set_path } %>

